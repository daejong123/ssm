<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daejong.vip.dao.VipRandDao">
    <select id="select" resultType="vipRankEntity">
        select * from  vip_rank where no = #{no}
    </select>

</mapper>